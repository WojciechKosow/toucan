{
  "name": "toucan-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Toucan JS monorepo: Node + Remotion renderer and shared SceneSpec package. The Spring Boot orchestrator lives at the repo root (pom.xml).",
  "workspaces": [
    "packages/*",
    "renderer"
  ],
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
