# API keys — only needed for generation (--topic). The --html dev path captures
# an existing file and needs no key. Set the one for the provider you use
# (--provider openai|anthropic; default openai).
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
