{
  "name": "@toucan/spec",
  "version": "0.1.0",
  "private": true,
  "description": "Shared SceneSpec contract: zod schema + TypeScript types for the element+edge+timeline IR. Imported by the renderer and (later) the frontend; mirrored by the Spring Boot validator.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "prettier --check \"src/**/*.ts\"",
    "test": "echo \"(SceneSpec schema + tests land in Section 1)\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "typescript": "^5.5.0"
  }
}
